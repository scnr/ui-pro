<%
   fragment_prefix = 'site_profile_attributes_' if !local_assigns.include?( :fragment_prefix )
%>
<%= content_tag 'th' do
    link_to "#{edit_site_path( @site )}#!/#{fragment_prefix}#{attribute}", &proc{ yield }
end %>
