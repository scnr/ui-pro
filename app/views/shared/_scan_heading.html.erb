<% if @sitemap_entry %>
    <h1>
    <small>
        Showing for:
        <%= link_to_url_with_external(
                    external: @sitemap_entry.url,
                    internal: sitemap_entry_url( @sitemap_entry ),
                    display_path_only: true
            )
        %>

        <%= link_to filter_params_without_page,
                    title: 'Remove page filter' do %>

            <small>
                <i class="text text-danger fa fa-minus-circle"></i>
            </small>
        <% end %>
    </small>
    </h1>
<% end %>
