<%= simple_form_for( [@site, @scan] ) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
        <%= f.input :name %>
        <%= f.input :description %>
        <%= f.input :enabled %>
    </div>

    <div class="form-actions">
        <%= f.button :submit %>
    </div>
<% end %>
