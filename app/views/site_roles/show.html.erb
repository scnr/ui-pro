<% content_for(:action_cable_channel, 'SiteRoleChannel') %>

<%
   title @site_role
   breadcrumb_add 'Sites', sites_path
   breadcrumb_add @site.url, site_path( @site )
   breadcrumb_add 'User roles', site_roles_path( @site )
%>

<% add_to_sidebar do %>
    <%= render partial: 'sidebar', locals: { submit: false } %>
<% end %>

<%= render @site_role, format: :html %>
