<h2>
    User roles

    <a href="#!/roles/new"
       title="New scan"
       class="btn btn-xs btn-primary"
       data-js="<%= new_site_role_path(@site) %>">
        <i class="fa fa-plus"></i>
    </a>
</h2>

<div class="lead">
    Use web application roles to perform scans as users with different levels of access.
</div>

<%= render partial: 'table' %>
