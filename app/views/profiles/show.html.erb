<% content_for(:action_cable_channel, 'ProfileChannel') %>

<%
    title @profile.name
    breadcrumb_add 'Profiles', profiles_path
%>

<% add_to_sidebar do %>
    <%= render partial: 'sidebar', locals: { submit: false } %>
<% end %>

<div class="row">
    <div class="col-md-12">
        <%= render @profile %>
    </div>
</div>
