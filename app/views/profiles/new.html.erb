<%
    title params[:action].capitalize
    breadcrumb_add 'Profiles', profiles_path

    if params[:action] == 'copy'
        breadcrumb_add @profile.name, profile_path( params[:id] )
    end
%>

<% add_to_sidebar do %>
    <%= render partial: 'sidebar' %>
<% end %>

<h1>New profile</h1>

<div class="row">
    <div class="col-md-12">
        <%= render 'form' %>
    </div>
</div>
